const { test } = require('../base/base.js');

test("Expect to have 0 options for signing up", async ({homePage: HomePage}) => {
  // Go to the ULTIMATE_QA_URL from your endpoint
  HomePage.getSiteLogo()
});
